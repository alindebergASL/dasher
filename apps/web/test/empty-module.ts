/**
 * Stands in for `server-only` under vitest. See `vitest.config.ts`: that
 * package's guarantee is enforced by the bundler at build time, not by
 * anything a unit test can observe.
 */
export {};
